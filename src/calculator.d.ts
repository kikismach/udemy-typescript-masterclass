// When creating a d.ts file using the declare keyword is optional
// Any code inside a d.ts file is a declaration
// If this declaration was in ts file itself then you would not have to export it explicitly
// and can use the declare keyword in that case
export declare function add(num1: number, num2: number): number;
